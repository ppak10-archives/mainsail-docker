services:
  mainsail:
    container_name: mainsail-server
    build:
      context: ./mainsail
      dockerfile: Dockerfile
    ports:
      - 7125:7125
      - 8080:80
    volumes:
      - ./mainsail/config.json:/usr/share/nginx/html/config.json
  
  klipper:
    container_name: klipper-server
    build:
      context: ./klipper
      dockerfile: Dockerfile
    # depends_on:
    #   - moonraker

    tty: true
